<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.android.peakfresh"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA"
                     android:required="false" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     android:maxSdkVersion="18"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MainTheme">
        <activity android:name=".ui.Main_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.Detail_Activity"
                  android:label="@string/title_activity_detail">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.Main_Activity" />
        </activity>

        <activity android:name=".ui.NewProduct_Activity"
                  android:label="@string/title_newProduct_Activity"
                  android:theme="@style/DialogTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.Main_Activity" />
        </activity>

        <activity android:name=".ui.SettingsActivity"
                  android:label="Settings">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.Main_Activity" />
        </activity>

        <provider
            android:authorities="com.example.android.peakfresh.data.ProductContentProvider"
            android:name=".data.generated.ProductContentProvider"
            android:exported="false"/>
        <provider
            android:authorities="com.example.android.fileprovider"
            android:name="android.support.v4.content.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
                <meta-data
                    android:name="android.support.FILE_PROVIDER_PATHS"
                    android:resource="@xml/file_paths" />
        </provider>

        <receiver android:name=".BootReceiver"
                  android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
        </receiver>
        <receiver android:process=":remote" android:name=".AlarmReceiver" />
        <service android:name=".NotificationService" />
    </application>

</manifest>